spring:
  application:
    name: sale

  datasource:
    url: jdbc:mysql://localhost:3306/saleSysDB
    username: root
    password: Password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true